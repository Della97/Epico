[package]
name    = "epico-logger"
version = "0.1.0"
edition = "2021"
description = "Structured runtime logger for epico — terminal + JSONL file"

[dependencies]
serde      = { version = "1", features = ["derive"] }
serde_json = "1"